  library(lubridate)#TidyR Dates
  library(readxl) #Read excel
  #library(xlsx) #Read excel
  library(ggplot2) #Pretty Pictures
  library(tidyr) 
  library(caret) #ML
  library(data.table) #Merging tables
  #library(RODBC) Databases
  library(dataMaid)# Data exploration
  library(DataExplorer) #Data exploration
  library(ggeffects) #Marginal effects
  #library(RANN) # Multiple imputation in CARET
  library(DMwR2) #SMOTE
  #library(PresenceAbsence) #Calibration Plots
  #library(pROC) #ROCs
  library(RSQLite) #SQL in R
  library(DBI) #Databases
  library(gnm) #Nonlinear regression
  library(R2OpenBUGS) # BUGS Models
  library(coda) #Analysis of BUGS output
  library(plotly) #3d plots
  library(nnet) #Multinomial models by neural network
  library(doParallel) #Parallel processing
  library(margins) #Marginal effects
  library(gnm) #Non linear GLMs
  library(reticulate) #Call python from R
  library(rpart) #Trees for exploring data
  library(rattle) #Displaying trees
  library(stringr)
  library(dplyr)
  library(lme4) #Random effect birth models
  library(mclogit) #Multinomial random effects models
  library(foreach)
  library(ROCR)
  library(doSNOW) # Foreach progress bars
  library(progress) #Foreach progress bars
  library(tcltk)
  library(cutpointr)
  #library(DMwR) # SMOTE
  library(MLmetrics) # Alternative ML tuning metrics
  library(gbm) #Needed for GBM model varImp
  library(corrplot)
  library(RColorBrewer)
  library(brms) #Bayesian MLMs
  library(cmdstanr)
  library(bayestestR) # Bayesian model comparisons with Bayes factor
  library(MASS) #Box Cox
  library(rjson) #JSON files
  library(splines) #CREATE B SPLINES
  library(lmerTest) #GLMM P values
  library(dtwclust) #Dynamic Time Warping Cluster Analysis
  